# Kept separate from pyproject.toml on purpose: packaging is a distinct roadmap item and
# introducing a pyproject.toml that declares no package would be misleading. Move this
# into [tool.pytest.ini_options] when the package metadata actually exists.
[pytest]
testpaths = tests
addopts = -q --strict-markers
